syntax = "proto3";

option go_package = "./grpc";

message Empty {}

message Request {
    int64 timestamp = 1;
    string from = 2;

}

message Reply {
    int64 timestamp = 1;
    string from = 2;
}

service RAServer {
    rpc RequestCS (Request) returns  (Empty);
    rpc ReplyCS (Reply) returns (Empty);

}

